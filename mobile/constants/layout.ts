/** Minimum recommended touch target (Apple HIG / Material). */
export const MIN_TOUCH_TARGET = 44;

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 20,
  xl: 24,
  xxl: 40,
} as const;

/** Keeps content readable on tablets without a bespoke layout. */
export const CONTENT_MAX_WIDTH = 560;
